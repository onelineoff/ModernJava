server.port=8888
server.ssl.enabled=false

# Cert generated from the command line with this command, then copied into src/main/resources/keystore directory.
# keytool -genkey -alias tomcat -storetype PKCS12 -keyalg RSA -keysize 2048 -keystore keystore.p12 -validity 3650

# TODO Recreate cert, modify parameters below
server.ssl.key-store-type=PKCS12
server.ssl.key-store=classpath:keystore/keystore.p12
server.ssl.key-store-password=onelineoff123
server.ssl.key-alias=tomcat

# Database parameters
spring.datasource.url=jdbc:postgresql://localhost:5432/modern_java
spring.datasource.username=onelineoff
spring.datasource.password=onelineoff

